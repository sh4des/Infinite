<?xml version="1.0"?>
<Container version="2">
  <Name>localbox</Name>
  <!--
    Repository: this is a CUSTOM image you build yourself (see README).
    Set this to wherever you pushed it, e.g.:
      ghcr.io/YOURUSER/localbox:latest   (recommended)
    or build it directly on the Unraid box and leave it as localbox:latest
    (in that case use the Compose Manager plugin instead of a registry pull).
  -->
  <Repository>localbox:latest</Repository>
  <Registry/>
  <Network>bridge</Network>
  <MyIP/>
  <Shell>sh</Shell>
  <Privileged>false</Privileged>
  <Support/>
  <Project/>
  <Overview>
    Infinite Jukebox for your own music library. Browse the music share mounted
    at /music, click any track, and it plays forever - the app analyses the song
    locally (librosa) and seamlessly loops between similar beats. Fully offline;
    no Spotify or YouTube. First play of a track takes a few seconds to analyse,
    then it is cached.
  </Overview>
  <Category>MediaApp:Music</Category>
  <WebUI>http://[IP]:[PORT:8080]/</WebUI>
  <Icon>https://raw.githubusercontent.com/twitter/twemoji/master/assets/72x72/267e.png</Icon>
  <ExtraParams/>
  <PostArgs/>
  <CPUset/>
  <DateInstalled/>
  <DonateText/>
  <DonateLink/>
  <Description>
    Infinite Jukebox for your local music library. Local analysis, no external
    services. Point /music at your library (read-only) and /data at appdata.
  </Description>

  <Config Name="WebUI Port" Target="8080" Default="8080" Mode="tcp"
          Description="Port for the web interface" Type="Port" Display="always"
          Required="true" Mask="false">8080</Config>

  <Config Name="Music Library" Target="/music" Default="/mnt/user/music"
          Mode="ro:shared" Description="Your music library (mounted read-only)"
          Type="Path" Display="always" Required="true" Mask="false">/mnt/user/music</Config>

  <Config Name="Cache / Data" Target="/data" Default="/mnt/user/appdata/localbox"
          Mode="rw" Description="Analysis + transcode cache (persisted so each track only analyses once)"
          Type="Path" Display="always" Required="true" Mask="false">/mnt/user/appdata/localbox</Config>

  <Config Name="AcoustID Key" Target="ACOUSTID_KEY" Default="" Mode=""
          Description="Optional. Acoustic fingerprinting fallback for untagged files. Free key at https://acoustid.org/new-application"
          Type="Variable" Display="always" Required="false" Mask="false"></Config>

  <Config Name="Force Transcode Extensions" Target="FORCE_TRANSCODE" Default="" Mode=""
          Description="Optional. Comma list of extensions to always transcode to mp3 for the browser, e.g. .flac,.wav . Leave blank to only transcode when needed."
          Type="Variable" Display="advanced" Required="false" Mask="false"></Config>
</Container>
