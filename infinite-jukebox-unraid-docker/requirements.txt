fastapi==0.115.6
uvicorn[standard]==0.34.0
librosa==0.10.2.post1
numpy==1.26.4
soundfile==0.12.1
audioread==3.0.1
mutagen==1.47.0
# Optional: acoustic fingerprinting fallback for untagged files.
# Only used when ACOUSTID_KEY is set AND the fpcalc binary is installed.
pyacoustid==1.3.0
